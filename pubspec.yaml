name: dart_inject
description: A very simple and easy to use dependency injection framework for Dart.
version: 0.1.0
homepage: https://github.com/meandmymac-de/dart_inject
issue_tracker: https://github.com/meandmymac-de/dart_inject/issues

environment:
  sdk: '>=2.8.1 <3.0.0'

#dependencies:
#  path: ^1.7.0

dev_dependencies:
  pedantic: ^1.9.0
  test: ^1.14.4
  test_coverage: ^0.4.2

deploy:
    provider: script
    script: pub publish -f
    on:
      branch: master